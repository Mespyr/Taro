# PLAN: annonymous functions will has strict definitions
# one in -> one out:
#    function signature always takes one type in and one type out
extern fn puti (I64)

fn out_res ((I64 -> I64) I64) =
  12 + swp call puti

fn main =
  [I64: dup *] 12 out_res
  []
